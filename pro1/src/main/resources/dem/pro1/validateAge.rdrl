package dem.pro1;

import java.lang.Number;

rule "validateAge"
	dialect "mvel"
	when
		GeneralDetail( age < 18 )
	then
		Messages fact0 = new Messages();
		fact0.setMessage( "Age below 18 is not eligible for the policy" );
		fact0.setMessageCode( 101 );
		insertLogical( fact0 );
end
